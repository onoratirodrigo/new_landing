---
import { stats } from "../data/stats";
import StatCounter from "./StatCounter.tsx";
---

<section class="bg-gradient-to-br from-brand-500 via-brand-600 to-brand-700">
  <div class="container-x py-14 md:py-16">
    <div class="grid grid-cols-2 md:grid-cols-4 gap-8 md:gap-12">
      {
        stats.map((s, i) => (
          <div class="text-center md:text-left">
            <div class="font-display font-extrabold text-white leading-none text-4xl md:text-5xl tracking-tighter2">
              <StatCounter
                client:load
                value={s.value}
                delay={i * 220}
              />
              {s.suffix && (
                <span class="text-white">{s.suffix}</span>
              )}
            </div>
            <div class="mt-2 text-sm md:text-base text-white/70">{s.label}</div>
          </div>
        ))
      }
    </div>
  </div>
</section>
